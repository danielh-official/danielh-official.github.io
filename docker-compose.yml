services:
  web:
    build: .
    ports:
      - "8080:80"
    develop:
      watch:
        - action: rebuild
          path: .
          ignore:
            - dist/
            - node_modules/
            - .git/
